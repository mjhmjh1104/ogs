<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= checker.file %>.cpp</title>
    <style>
        code {
            white-space: pre-wrap;
        }
    </style>
</head>
<body>
    <h1><%= checker.file %>.cpp</h1>
    <%= checker.description %><br>
    <code><%= code %></code><br>
    <a href="/checkers">Checkers</a>
<% if (user && user.admin) { %>
    <form method="POST" action="/checkers/delete/<%= checker.file %>" accept-charset="utf-8">
        <button type="submit">Remove</button>
    </form>
<% } %>
</body>
</html>